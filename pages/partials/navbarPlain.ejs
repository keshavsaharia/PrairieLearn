<nav class="navbar navbar-expand-sm navbar-dark bg-dark navbar-static-top" role="navigation">
  <a class="navbar-brand"  href="<%= plainUrlPrefix %>">PrairieLearn</a>

  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbar-collapse">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbar-collapse">
      <ul class="nav navbar-nav mr-auto" id="main-nav">
        <li class="nav-item <% if (navPage == 'home') { %>active<% }%>">
            <a class="nav-link" href="<%= plainUrlPrefix %>">Home</a>
        </li>
        <% if (is_administrator) { %>
        <li <% if (navPage == "admin") { %>
                class="nav-item active"
            <% } else {%>
                class="nav-item"
            <% } %>>
            <a class="nav-link" href="<%= plainUrlPrefix %>/administrator/overview">
                Admin
            </a>
        </li>
        <% } %>
      </ul>
      <% if (typeof authn_user == 'undefined') { %>
      <span class="navbar-text">(No user)</span>
      <% } else if (is_administrator) { %>
      <span class="navbar-text"><i class="fa fa-user-circle"></i> admin</span>
      <% } else { %>
      <span class="navbar-text"><%= authn_user.name %></span>
      <% } %>

      <form class="form-inline my-2 my-sm-0">
        &nbsp;&nbsp;
        <% if (devMode) { %>
            <a class="btn btn-outline-primary" href="<%= urlPrefix %>/loadFromDisk">Load from disk</a>
        <% } %>
        &nbsp;&nbsp;
        <%- include('logout'); %>
      </form>
  </div>
</nav>
